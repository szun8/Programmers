#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    int answer = stoi(s);
    // 부호까지 처리해주는 string to int ()
    return answer;
}

int main() {
    solution("-1234");
    return 0;
}